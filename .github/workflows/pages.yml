name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - uses: actions/configure-pages@v6
        with:
          enablement: true
      - run: bun install --frozen-lockfile
      - name: Build versioned documentation
        env:
          DOCS_BASE: /Form/
        run: bun run docs:build
      - name: Build the hosted Playground
        env:
          A3S_FORM_BASE: /Form/playground/
        run: bun run playground:build
      - name: Combine static sites
        run: |
          mkdir -p apps/docs/doc_build/playground
          cp -R playground-dist/. apps/docs/doc_build/playground/
      - uses: actions/upload-pages-artifact@v5
        with:
          path: apps/docs/doc_build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
